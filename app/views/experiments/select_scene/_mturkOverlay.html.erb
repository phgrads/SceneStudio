<%= render '/experiments/largeimg' %>
<div id="taskInstructions" class="wholescreenOverlay">
  <div align="center" class="taskInstructions">
    <div style='text-align:left'>
      <p>You will be shown <%=@entries.length%> sentences.  For each sentence,
        you will be shown <%=@conf['nChoices']%> images.  Please select
        the image that best matches the sentence.
      </p>
      <p>
        Each sentence should take about <%=@taskTime['sceneTime'] %> to complete.
        The task should take about <%=@taskTime['taskTime'] %> in total.
      </p>
     </div>

  <div align="center">
    <table class="exampleTable">
      <thead>
    	<th>
    	  <div align="center">
    	    Example
    	  </div>
    	</th>
      </thead>
      <tbody>
    	<tr>
    	  <td>
    	    <b>Description:</b> I see am empty book shelf and i desk that has nothing on it. In the corner of the room there are shelves that have nothing on them.
    	  </td>
    	</tr>
    	<tr>
          <td>
    	    <div align="center">
	      <% checkNr = 4 %>
    	      <%for i in 0...@conf['nChoices']%>
    	      <table style="display:inline-block">
    	      	<tr>
    	      	  <td>
    	      	    <div align="center" class="mturkExample">
    	      	      <%= image_tag( @conf['exampleImagesPath'] + 'example-0-'+i.to_s+'.png', { width: 400} )%>
		      <!-- can add enlarge later, if it works well -->
    	      	    </div>
    	      	  </td>
    	      	</tr>
    	      	<tr>
    	      	  <td>
    	      	    <!-- checkmark or x-mark -->
		    <font color=<%= (i == checkNr) ? "green" : "red" %>>
    	      	      <%= (i == checkNr) ? raw("&#x2714;") : raw("&#x2718;") %>
		    </font>
    	      	  </td>
    	      	</tr>
    	      </table>
    	      <%end%>
    	    </div>
          </td>
    	</tr>
      </tbody>
    </table>
  </div>
  <div  class="center">
    <input type="button" id="startButton" class="largeButton" value="Start"/>
  </div>
  </div>
</div>

<div id="mturkOverlay" class="wholescreenOverlay" style="display:none">
    <div id="instructionsTextDiv" class="center largeFont">
    <p>Please select the scene that is most similar to the following description</p>
    </div>
    <div id="sceneDescription" class='textDescription'>
    </div>
    
    <div align="center">
      <%for i in 0...@conf['nChoices']%>
      <div style="display:inline-block" id='<%="sceneImageFrame#{i}"%>' >
	<%= image_tag '', id: "sceneImage#{i}", width: 600 %>
	<!-- the class: "enlarge" is distracting (at least for now) -->
      </div>
      <%end%>
    </div>

    <div align="center">
    <p>Click on the scene to continue</p>
    </div>

    <!-- <div class="container"> -->
    <!-- <div class="row"> -->
    <!-- <%for i in 0...@conf['nChoices']%> -->
    <!-- <div class="col-md-2 col-sm-2 col-xs-2"> -->
    <!--   <div class="thumbnail" id='<%="sceneImageFrame#{i}"%>' > -->
    <!--     <%= image_tag '', id: "sceneImage#{i}" %> -->
    <!--   </div> -->
    <!-- </div> -->
    <!-- <%end%> -->
    <!-- </div> -->
    <!-- </div> -->

    <!-- <div class="center"> -->
    <!-- <p>Click the "Next" button when you're ready to continue.</p> -->
    <!-- <input type="button" id="nextButton" class="largeButton" value="Next"/> -->
    <!-- </div> -->
</div>
