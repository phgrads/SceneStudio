<%= render '/experiments/largeimg' %>
<div id="taskInstructions" class="wholescreenOverlay">
  <div align="center" class="taskInstructions">
    <div style='text-align:left'>
      <p>You will be shown <%=@entries.length%> sentences.  For each sentence,
        you will be shown <%=@conf['nChoices']%> images.  Please select
        the image that best matches the sentence.
      </p>
      <p>
        Each sentence should take about <%=@taskTime['sceneTime'] %> to complete.
        The task should take about <%=@taskTime['taskTime'] %> in total.
      </p>
     </div>
  <div align="center">
    <table class="exampleTable">
      <thead>
      <th>Example</th>
      </thead>
      <tbody>
	<tr>
          <td>
            <div class="wrap">
              <%for i in 0...@conf['nChoices']%>
	      <td>
	        <div algin="center" class="mturkExample">
		  <%= image_tag( @conf['exampleImagesPath'] + 'example-0-'+i.to_s+'.png', { width: 400, class: "enlarge"} )%>
  		</div>
	      </td>
	      <%end%>
            </div>
            <!-- <div class="exampleTable"> -->
            <!--   <div class="row"> -->
            <!--     <%for i in 0...@conf['nChoices']%> -->
	    <!-- 	  <td> -->
	    <!-- 	    <%= image_tag( @conf['exampleImagesPath'] + 'example-0-'+i.to_s+'.png', { width: 400, class: "enlarge"} )%> -->
	    <!-- 	  </td> -->
            <!--     <\!-- <div class="col-md-2 col-sm-2 col-xs-2"> -\-> -->
            <!--     <\!--     <div class="thumbnail" id='<%="exSceneImageFrame#{i}"%>' > -\-> -->
            <!--     <\!--         <%= image_tag '', id: "exSceneImage#{i}" %> -\-> -->
            <!--     <\!--     </div> -\-> -->
            <!--     <\!-- </div> -\-> -->
            <!--     <%end%> -->
            <!--   </div> -->
            <!-- </div> -->
          </td>
	</tr>
      </tbody>
    </table>
  </div>
  <div  class="center">
    <input type="button" id="startButton" class="largeButton" value="Start"/>
  </div>
  </div>
</div>

<div id="mturkOverlay" class="wholescreenOverlay largeFont" style="display:none">
    <div id="instructionsTextDiv" class="center">
    <p>Please select the scene that is most similar to the following description</p>
    </div>
    <div id="sceneDescription" class='textDescription'>
    </div>
    <div class="container">
        <div class="row">
          <%for i in 0...@conf['nChoices']%>
            <div class="col-md-2 col-sm-2 col-xs-2">
              <div class="thumbnail" id='<%="sceneImageFrame#{i}"%>' >
                <%= image_tag '', id: "sceneImage#{i}" %>
              </div>
            </div>
          <%end%>
        </div>
    </div>
    <div class="center">
    <p>Click the "Next" button when you're ready to continue.</p>
    <input type="button" id="nextButton" class="largeButton" value="Next"/>
    </div>
</div>
