<%= render '/experiments/largeimg' %>
<div id="taskInstructions" class="wholescreenOverlay">
  <div align="center" class="taskInstructions">
    <div style='text-align:left'>
      <p>You will be shown <%=@entries.length%> short descriptions of simple indoor scenes.
        For each description, you will be asked to construct a scene that matches the description
        by finding appropriate objects and placing them in a room.
      </p>
      <p>
        Each scene should take about <%=@taskTime['sceneTime'] %> to complete.
        The task should take about <%=@taskTime['taskTime'] %> in total.
      </p>
    </div>
  <div align="center">
    <table class="exampleTable">
      <thead>
      <th></th>
      <th>Example 1</th>
      <th>Example 2</th>
      </thead>
      <tbody>
      <tr>
        <th>Scene description</th>
        <td>
          There is a table and there are four chairs.
        </td>
        <td>
          There is a bed and there is a nightstand next to the bed.
          <br/>
          There is a floor lamp next to the bed.
        </td>
      </tr>
      <tr>
        <th>Constructed scene</th>
        <td>
          <div align="center" class="mturkExample">
            <%= image_tag( @conf['exampleImagesPath'] + 'example1.png', { width: 400, class: "enlarge" } ) %>
          </div>
        </td>
        <td>
          <div align="center" class="mturkExample">
            <%= image_tag( @conf['exampleImagesPath'] + 'example2.png', { width: 400, class: "enlarge" } ) %>
          </div>
        </td>
      </tr>
      </tbody>
    </table>
  </div>
  <br/>
  <div  class="center">
    <input type="button" id="startButton" class="largeButton" value="Start"/>
  </div>
  </div>
</div>

<div id="mturkOverlay" class="mturkOverlay" oncontextmenu="$('#canvas').focus(); return false;"
     onclick="$('#canvas').focus();" style="display:none">
  <div id="editInstructions" class="editInstructions">
    <p id="prompt" class="prompt">Put mouse here for instructions</p>
    <div id="instructions" class="instructions">
      <p>Place objects in the room to create a scene that best resembles the text description.</p>
      <p>Find objects using the search bar on the right.
        Left click on search results and left click in the scene to place the object. Left-click drag to move objects.</p>
      <p>When an object is selected, drag the blue circle around it to rotate it, and drag the yellow arrows to resize it.
        Keyboard shortcuts: up and down arrow to resize, left and right arrow to rotate.</p>
      <p>To move the camera use right-click dragging and the mouse wheel, or use the camera control button in the top left corner.</p>
      <p>Please make sure the final arrangement of objects matches the description.</p>
      <p>Click the "Save" button when you're done to go on to the next scene.</p>
    </div>
  </div>
</div>

<%= render 'entryOverlay' %>
