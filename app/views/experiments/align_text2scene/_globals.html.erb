<script type='text/javascript'>
    window.globals = {
        entries: <%= raw(@entries.to_json) %>,
        conf: <%= raw(@conf.to_json) %>,
        <% if @via_turk %>
        assignmentId: "<%= @assignment.mtId %>",
        <% end %>
    };
    //var store;
    //$(function() { store = new Persist.Store('align-text2scene'); });
</script>